const hasEscrowExpired = (escrowId) => {
  return new Promise((resolved, rejected) => {});
};

const signEscrowSignature = (escrowData) => {
  return new Promise((resolved, rejected) => {});
};

const getEscrowDetails = (escrowId) => {
  return new Promise((resolved, rejected) => {});
};

const getEscrowLogs = (escrowId) => {
  return new Promise((resolved, rejected) => {});
};

module.exports = {
  hasEscrowExpired,
  signEscrowSignature,
  getEscrowDetails,
  getEscrowLogs,
};
